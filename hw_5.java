public class hw_5 {
    public static void main (String args []){
        int numerator = 1;
        int denominator = 2;
        int numeratorSecond = 2;
        int denominatorSecond = 5;
        int finalNumerator = (numerator * denominatorSecond) + (denominator + numeratorSecond);
        int finalDenominator = (denominator * denominatorSecond); 



        System.out.println("The sum of " + numerator + "/" + denominator + " + " + numeratorSecond + "/" + denominatorSecond + " = " + finalNumerator + "/" + finalDenominator);
    }
}
